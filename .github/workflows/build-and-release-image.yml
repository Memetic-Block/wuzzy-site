name: Build & Publish Wuzzy Site Static Stage Image

on:
  push:
    branches: master
    tags:
      - v*
env:
  IMAGE_NAME: wuzzy-site

jobs:
  push:
    runs-on: self-hosted
    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Build image
        env:
          VITE_REGISTRY_PROCESS_ID: "PJVif9KTSNZ2pYrt18Wn976SJjCLuvs3dj7r5Oh2xXQ"
          VITE_PRIMARY_NEST_ID: "1X_nt5ctoJTw6Dc3M6x34_lFTWGTl-jhW8MY7Vff4fA"
          VITE_HYPERBEAM_ENDPOINT: "https://wuzzy-hyperbeam.hel.memeticblock.net"
          VITE_SEARCH_API_URL: "https://wuzzy-search-api-stage.hel.memeticblock.net"
        run: |
          VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')
          docker build . \
            --file Dockerfile \
            --tag $IMAGE_NAME \
            --label "runnumber=${GITHUB_RUN_ID}"

      - name: Log in to registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u $ --password-stdin

      - name: Push image
        run: |
          IMAGE_ID=$CONTAINER_REGISTRY_ADDR/${{ github.repository_owner }}/$IMAGE_NAME
          GHCR_IMAGE_ID=ghcr.io/${{ github.repository_owner }}/$IMAGE_NAME

          # Change all uppercase to lowercase
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')
          GHCR_IMAGE_ID=$(echo $GHCR_IMAGE_ID | tr '[A-Z]' '[a-z]')

          # Strip git ref prefix from version
          VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')

          # Strip "v" prefix from tag name
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')

          # Use Docker `latest` tag convention
          [ "$VERSION" == "master" ] && VERSION=stage
          echo IMAGE_ID=$IMAGE_ID
          echo GHCR_IMAGE_ID=$GHCR_IMAGE_ID
          echo VERSION=$VERSION

          docker tag $IMAGE_NAME $IMAGE_ID:${GITHUB_SHA}
          docker tag $IMAGE_NAME $GHCR_IMAGE_ID:${GITHUB_SHA}
          docker push $IMAGE_ID:${GITHUB_SHA}
          docker push $GHCR_IMAGE_ID:${GITHUB_SHA}
      
      - name: Deploy new version
        run: |         
          sed -i "s/\[\[ .commit_timestamp \]\]/${{ github.event.head_commit.timestamp }}/g" operations/wuzzy-site-static-stage.hcl
          sed -i "s/\[\[ .commit_sha \]\]/${{ github.sha }}/g" operations/wuzzy-site-static-stage.hcl
          
          nomad job run operations/wuzzy-site-static-stage.hcl